{
  "_comment": "ultraloop /goal 게이트 — 대상 레포 .claude/settings.json 의 hooks.Stop 에 병합. bootstrap_repo.sh 가 __ULTRALOOP_SKILL_DIR__ 를 이 스킬의 절대경로로 치환해 설치한다(런타임에 CLAUDE_SKILL_DIR 미설정이므로 절대경로 필수).",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash __ULTRALOOP_SKILL_DIR__/assets/hooks/goal-stop-gate.sh"
          }
        ]
      }
    ]
  }
}
