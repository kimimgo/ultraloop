{
  "_comment": "ultraloop /goal gate — merge into hooks.Stop of the target repo .claude/settings.json. bootstrap_repo.sh installs it by replacing __ULTRALOOP_SKILL_DIR__ with the absolute path of this skill (CLAUDE_SKILL_DIR is unset at runtime, so an absolute path is required).",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash __ULTRALOOP_SKILL_DIR__/assets/hooks/goal-stop-gate.sh"
          }
        ]
      }
    ]
  }
}
