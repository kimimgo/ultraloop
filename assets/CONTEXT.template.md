# Project context

<!--
  The at-a-glance brief a new session (or a new collaborator) needs before touching anything.
  This is the local mirror of the board README (the SoT). Edit it here, then publish to the board:
      bash <gh-roadmap>/scripts/roadmap_readme.sh set --file .claude/.ultraloop-context.md
  Written in the product's working language — collaborators read it. Keep tool/automation names OUT of it.
-->

## What this is
One or two sentences: what the product is and the north star it serves.

## Repositories (this board spans them)
- `owner/repo-a` — role (e.g. control plane / API)
- `owner/repo-b` — role
- `owner/repo-c` — role

## People
- Who is collaborating, who approves scope, who owns deploys.

## Special rules for this project
- Non-obvious constraints that are NOT derivable from the code or the README
  (domain rules, a "never touch X", a required review path, an environment quirk).

## Where the truth lives
- Roadmap/scope/status: the board (single source of truth).
- Progress view: `PROGRESS.md` (read-only, regenerated).
